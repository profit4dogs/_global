---
status: draft   # draft -> locked -> superseded-by <path>
updated: YYYY-MM-DD
---

# SPEC — <feature/component name>

<!-- A spec marked `locked` is implemented exactly as written.
     Changing a locked spec requires a DECISIONS.md entry first, then a new spec version.
     Fixes to locked-spec implementations are promoted as complete units, not piecemeal. -->

## Purpose

<!-- One paragraph. What this component does and why it exists. -->

## Behavior

<!-- The contract. Numbered, testable statements. Pseudocode welcome.
     Every statement should be checkable against the implementation. -->

1.

## Invariants

<!-- Things that must never happen / always hold, stated explicitly.
     e.g. "minVisibleDurationSeconds gates exit/fade only; it never gates word appearance." -->

## Out of scope

<!-- What this spec deliberately does not cover, to prevent scope creep during implementation. -->

## Acceptance

<!-- How we know the implementation matches: specific inputs -> expected observable outputs. -->
